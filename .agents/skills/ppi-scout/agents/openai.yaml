interface:
  display_name: "PPI Scout"
  short_description: "Plan and run reproducible local PPI panels"
  default_prompt: "Use $ppi-scout to plan and run a reproducible local protein interaction panel with matched controls."

policy:
  allow_implicit_invocation: true
